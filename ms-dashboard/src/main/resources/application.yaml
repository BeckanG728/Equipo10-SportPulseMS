server:
  port: 8089
  servlet:
    context-path: /api

spring:
  application:
    name: ms-dashboard

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
